name: aws
source: github.com/deis/deis

configure:
  vars:
    - name: AWS_ACCESS_KEY_ID
      question: "Enter your AWS key:"

    - name: AWS_SECRET_ACCESS_KEY
      question: "Enter your AWS secret key:"

up:
  script: ./contrib/create

info:
  script: ./info

down:
  script: ./destroy
